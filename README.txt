CONNECTFOUR ------ README

In order to run the server for connect Four, run the file src/cf/server/Server.java.

In order to run a client for connect Four, run the file src/cf/Client/Client.java.

Both the client and server have a TUI to do the rest, if its unclear which command are possible for a client, type "help" and a list of possible commands will be shown.